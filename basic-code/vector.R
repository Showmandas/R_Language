#A vector is simply a list of items that are of the same type.


#strings
color <- c('orange','brown','blue')
color


#numeric

numbers <- c(6,2,3,4,5,0,1)
numbers

#sort
sort(numbers)


numlist <- 2:5
numlist

#vector length
length(numlist)

#access
color[1]
numbers[c(1,3)]
